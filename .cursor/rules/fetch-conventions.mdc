---
description: 规范前端页面、组件与状态管理中的 API 调用方式，优先使用 @/lib/api-client，避免直接裸调 fetch，统一错误语义与提示处理。
alwaysApply: true
---

## 前端调用规范（api-client）

前端（页面、组件、store）调用本项目 API 时，优先使用 `@/lib/api-client`，避免直接裸调 `fetch`，以确保错误语义一致。

```ts
import { apiFetch, ApiError } from "@/lib/api-client"
import { API_ERRORS } from "@/lib/api-error"

try {
  const data = await apiFetch<Project[]>("/api/projects")
  // 使用 data
} catch (err) {
  if (err instanceof ApiError) {
    // 前端展示优先使用 message
    toast.error(err.message)

    // 分支判断使用 error code（ApiError.code）
    if (err.code === API_ERRORS.NOT_FOUND.code) {
      // ...
    }
  } else {
    toast.error("请求失败，请稍后重试")
  }
}
```

