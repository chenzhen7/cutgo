---
description: LLM 调用规范：必须使用项目统一封装的 getLLMProvider，禁止直接裸调 fetch 请求 AI API
globs: app/api/**/*.ts
alwaysApply: false
---

# LLM 调用规范

所有 API 路由调用 LLM 时，**必须**使用项目封装的统一入口 `getLLMProvider()`，**禁止**直接读取环境变量并裸调 `fetch`。

## 正确用法

```ts
import { getLLMProvider } from "@/lib/ai/llm"
import { llmNotConfigured, llmInvalidResponse } from "@/lib/api-error"

const llmProvider = await getLLMProvider()

// 未配置时返回标准 422 错误
if (!llmProvider) {
  return llmNotConfigured()
}

// 调用 LLM
const result = await llmProvider.chat({
  messages: [{ role: "user", content: prompt }],
})

// 结果处理
const text = result.content
```

## 禁止写法

```ts
// ❌ 禁止：直接读取环境变量 + 裸 fetch
const apiKey = process.env.OPENAI_API_KEY
const baseUrl = process.env.OPENAI_BASE_URL || "https://api.openai.com/v1"
const model = process.env.OPENAI_MODEL || "gpt-4o-mini"

const response = await fetch(`${baseUrl}/chat/completions`, {
  method: "POST",
  headers: { Authorization: `Bearer ${apiKey}` },
  body: JSON.stringify({ model, messages }),
})
```

## LLM 未配置的错误处理

- 直接调用路径：`getLLMProvider()` 返回 `null` 时，直接 `return llmNotConfigured()`
- 间接调用路径（内部函数中）：`throw new Error(ERR_LLM_NOT_CONFIGURED)`，在外层 catch 中判断并返回

```ts
import { ERR_LLM_NOT_CONFIGURED } from "@/lib/api-error"

// 内部函数
async function callLLM(...) {
  const llmProvider = await getLLMProvider()
  if (!llmProvider) throw new Error(ERR_LLM_NOT_CONFIGURED)
  ...
}

// 外层 catch
} catch (err) {
  if ((err as Error).message === ERR_LLM_NOT_CONFIGURED) {
    return llmNotConfigured()
  }
  return internalError("调用失败", (err as Error).message)
}
```

## LLM 返回内容解析

解析前去掉可能的 markdown 代码块包裹，并用 `try/catch` 保护：

```ts
function parseLLMJson<T>(raw: string): T {
  let text = raw.trim()
  text = text.replace(/^```(?:json)?\s*/i, "").replace(/\s*```\s*$/, "").trim()
  return JSON.parse(text) as T
}
```

## Provider 支持情况

`getLLMProvider()` 从数据库配置中读取，自动适配以下所有 Provider：
`openai` / `deepseek` / `google` / `anthropic` / `qwen` / `doubao` / `zhipu` / `custom-openai`
