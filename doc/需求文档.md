这是一份为您定制的 **ScriptForge (Stage 1) 详细产品需求文档 (PRD)**。作为 Java 背景的开发者，这份文档在逻辑结构上参考了系统分析的严谨性，同时结合了 AI 原生应用（AI-Native）的交互特性。

***

# 📖 产品需求文档 (PRD) v1.0

## 1. 文档概览

* **产品名称**：ScriptForge (Project FrameMind)

* **产品定位**：AI 驱动的短剧全链路工业化生产工具。

* **核心价值**：通过“文本-剧本-分镜-视频”的自动化管线，将小说改编短剧的效率提升 10 倍以上。

***

# 📂 模块 1：项目管理&#x20;

## 1.1 模块定位

作为生产线的“中控室”，负责定义项目的全局配置（如画幅、风格、语种），并管理所有原始素材（小说原文）与生成资产（角色卡、场景库）的关联关系。

***

## 1.2 核心功能拆解

### 1.2.1 项目创建与元数据设置 (Project Ingest)

用户发起新创作任务时的初始化配置。

* **项目属性配置**：

  * **项目名称**：自定义（如：*《龙王回归》第1-10集*）。

  * **描述**

  * **标签**

  * **封面：默认第一帧**

  * **目标平台预设**：

    * **抖音/TikTok** (9:16, 1080x1920)

    * **视频号/快手** (3:4, 1080x1440)

    * **YouTube/横屏** (16:9, 1920x1080)

  * **预计时长**：单集（30s / 60s / 90s / 自定义），影响 AI 拆解剧本的节奏感。



### 1.2.2 视觉风格管理器 (Visual Style Manager)

**这是 AI 绘画一致性的关键。** 在项目开始前锁定视觉参数，防止视频风格“打架”。

* **风格预设 (Preset)**：

  * **写实类**：都市霸总、古装仙侠、硬核科幻。

  * **艺术类**：日漫风、美漫风格、赛博朋克、水墨风。

* **全局提示词 (Global Negative Prompts)**：

  * 自动注入通用负面词（如：`bad anatomy, text, watermark, low quality`）。

* **参考图上传 (Style Reference)**：

  * 支持上传一张“风格参考图”，系统通过 IP-Adapter 提取色彩和笔触，确保全剧画面调性统一。

### 1.2.3 资产库 (Asset Library)

项目内所有生成内容的集中仓库，支持跨章节调用。

* **角色库 (Cast)**：展示本项目已定义的角色模型（Seed 值、人脸特征、固定服装）。

* **场景库 (Scene Assets)**：存储已生成的固定背景（如：*总裁办公室、荒野废墟*），确保场景在不同镜头间不穿帮。

* **素材复用逻辑**：支持将 A 项目的角色/场景直接导入 B 项目（系列剧必备）。

### 1.2.4 项目看板&#x20;

* **列表/网格视图**：展示所有创作中的短剧项目。

* **关键信息外显**：项目名称、封面图（自动抓取第一镜）、总集数/总时长、最后修改时间。

***

## 1.3 界面交互设计 (UI/UX)

***

## 1.4 数据结构设计 (Logic Architecture)

作为 背景开发者，您可以参考以下伪代码定义的 **ProjectEntity** 核心字段：

Java

```plain&#x20;text
public class Project {
    private String projectId;        // 唯一IDprivate String title;            // 项目名private AspectRatio ratio;       // 枚举：NINE_SIXTEEN, SIXTEEN_NINEprivate String globalStyleId;    // 关联风格模型ID (如: "realistic_v3")private List<String> sceneAssets; // 关联场景图片路径private List<Character> cast;    // 关联角色对象列表private ProjectStatus status;    // 状态：DRAFT, SCRIPTING, RENDERING, DONEprivate Config aiConfig;         // 存储 Base Prompt, Negative Prompt, Seed 等
}
```

***

## 1.5 业务规则 (Business Rules)

1. **分辨率锁定**：项目一旦创建并开始生成画面，画幅比例（Ratio）禁止修改，防止后续生成的图片比例失调。

2. **版本快照**：支持“分支功能”。用户可以针对同一部小说创建两个分支项目（如：一个“写实风”，一个“动漫风”），对比效果。

3. **自动存档**：任何剧本的微调应实时触发后端 SQLite 或 Redis 的增量保存，防止浏览器崩溃导致灵感丢失。

***

## 1.6 后续建议

本模块是\*\*“地基”\*\*。

**您想让我接下来为您细化哪一部分？**

* **A. 模块 2 & 3（小说导入与剧本生成）**：这是技术含量最高的地方，涉及 LLM 如何精准拆解剧情冲突。

* **B. 模块 4（角色一致性系统）**：这是视觉表现力的核心，涉及如何通过 AI 锁定“同一张脸”。

